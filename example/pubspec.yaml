name: example
description: "Example for pressable_button - A customizable 3D animated pressable button for Flutter with smooth depth and glow effects."
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  pressable_button:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
