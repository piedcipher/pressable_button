name: pressable_button
description: "A customizable 3D animated pressable button for Flutter with smooth depth and glow effects."
version: 0.0.1
homepage: https://tirth.today
repository: https://github.com/piedcipher/pressable_button
issue_tracker: https://github.com/piedcipher/pressable_button/issues

topics:
  - flutter
  - button
  - animation
  - ui
  - widget

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
